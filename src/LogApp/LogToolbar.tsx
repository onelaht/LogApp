export default function LogToolbar() {
    return (
        <>
            <select>
                <option value="test"> Select Account </option>
            </select>
            <button
                disabled={true}
            >Upload</button>
        </>
    )
}